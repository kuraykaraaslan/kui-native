/**
 * @deprecated Use `Input` (KuiReact's name). Kept so existing imports keep
 * working; note that `className` now targets the wrapper, as in KuiReact —
 * use `inputClassName` for the field.
 */
export { Input as TextInput } from "./Input";
export type { InputProps as TextInputProps } from "./Input";
