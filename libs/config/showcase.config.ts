/** Branding constants for the showcase host (KUIREACT's libs/config analogue). */
export const showcaseConfig = {
  name: "KUInative",
  version: "0.1.0",
  tagline: "Minimal React Native component library",
} as const;
